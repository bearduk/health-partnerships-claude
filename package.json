{
  "name": "keele-health-partners",
  "private": true,
  "scripts": {
    "dev": "browser-sync start --server --files 'index.html, assets/**/*, robots.txt' --no-notify --no-open",
    "dev:open": "browser-sync start --server --files 'index.html, assets/**/*, robots.txt' --no-notify"
  },
  "devDependencies": {
    "browser-sync": "^3.0.0"
  }
}
